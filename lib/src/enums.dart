// Enums that match the native code
enum CameraFacing { back, front }

enum FilterType {
  none,
  sepia,
  grayScale,
  invert,
  brightness,
  contrast,
  saturation,
  gamma,
  monoChrome,
}

enum FlashMode { off, on, auto }

enum ResolutionPreset { low, medium, high, veryHigh, ultraHighs }
