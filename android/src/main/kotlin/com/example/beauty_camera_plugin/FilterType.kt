package com.example.beauty_camera_plugin

/**
 * Enum defining the available filter types for the camera preview
 */
enum class FilterType {
    NONE,
    SEPIA,
    GRAYSCALE,
    INVERT,
    BRIGHTNESS,
    CONTRAST,
    SATURATION,
    GAMMA,
    MONOCHROME
}
